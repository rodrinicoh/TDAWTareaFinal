Abrir mongodb para que se inicie el mongodbserver (confirmarlo arriva a la izquierda en "connect")

Ir a la carpeta donde se encuentre Server.js y ejecutar:
node Server.js

Ir a la carpeta con ela rchivo Index.html y ejecutar:
http-server

Los archivos package.json se hicieron a mano al igual que los Dockerfile y docker-compose

los archivos packege-lock.json se crean yendo a la carpeta con los packege.json y ejecutando:
npm update

No olvidar iniciar el servidor de mongo en C:\Program Files\MongoDB\Server\7.0\bin
hacer click en mongod